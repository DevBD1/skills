# <service-or-module-name>

One-paragraph purpose: what this service/module does and where it sits in the system.

## Setup

Steps to get this service running from a fresh clone, including dependencies.

## Environment

| Variable | Required | Purpose |
| --- | --- | --- |
| `EXAMPLE_VAR` | yes | What it configures. |

## Commands

```sh
<run-command>        # run locally
<test-command>       # run tests
<validation-command> # smallest relevant validation
```

## Endpoints / Interface

Public endpoints, CLI commands, or exported APIs — the surface other services and agents rely on.

## Operational Notes

Deployment specifics, health checks, known failure modes, rollback notes. Repo-wide process lives in root docs, not here.
